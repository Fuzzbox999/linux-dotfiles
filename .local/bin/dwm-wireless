#!/bin/sh

ssid() {
	ssid=$(iw wlp3s0 info|grep ssid|cut -d " " -f 2|xargs)
	if [ -z $ssid ]; then
		color="^c#ff0000^"
		state="  "
		endcolor="^d^"
	else
		color=""
		state="  "
		endcolor=""
	fi
	echo "$color$state$endcolor"
}

echo $(ssid)
