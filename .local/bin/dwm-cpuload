#!/bin/sh

. $HOME/.cache/wal/colors.sh
. $HOME/.local/tmp/cpu-load

high=75

if [ "$load" -gt $high ]; then
	color="^c#ff0000^"
	load=$load
	endcolor=""
else
	color="^c$background^"
	load=$load
	endcolor=""
fi

echo "^b$foreground^$color  $load%"

